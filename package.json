{
  "name": "pintia-mcp-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Independent Pintia/PTA MCP server + Codex skill monorepo",
  "license": "MIT",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
